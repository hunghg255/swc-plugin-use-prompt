{
  "name": "swc-plugin-use-prompt",
  "version": "0.1.0",
  "description": "A satirical attempt at making a `use prompt` directive for React and NextJS.",
  "author": "Pranav Nutalapati",
  "license": "ISC",
  "keywords": [
    "swc-plugin"
  ],
  "bin": {
    "use-prompt": "scripts/use-prompt.mjs"
  },
  "main": "target/wasm32-wasip1/release/use_prompt.wasm",
  "scripts": {
    "prepublishOnly": "cargo build-wasi --release",
    "build": "npm run build:wasm",
    "build:wasm": "cargo build-wasi --release"
  },
  "type": "module",
  "files": [],
  "preferUnplugged": true,
  "dependencies": {
    "@next/env": "^15.0.1",
    "@swc/core": "^1.7.40",
    "openai": "^4.68.4"
  }
}
